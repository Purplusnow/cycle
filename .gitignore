.env
__pycache__/
.venv

# 수집 자료. 저장소에 담지 않는다 — 자동 배포는 캐시로 나른다.
data/

# 빌드 산출물. 배포는 GitHub Actions 가 매번 새로 굽는다.
dist/

# 모델은 저장소에 **둔다**. 자동 배포가 매번 재학습하지 않고 그대로 쓰고,
# 그래야 어느 시점의 모델이 그 예상을 냈는지 되짚을 수 있다.
!models/
