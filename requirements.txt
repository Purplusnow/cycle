# 버전을 고정한다.
#
# 모델은 joblib 피클로 저장했다가 다음 실행에서 되읽는다. 절인 쪽과 푸는 쪽의
# scikit-learn 이 다르면 사설 클래스가 사라져 그대로 깨진다.
#
# 경마(horseai)·경정(boatai)과 같은 가상환경을 공유하므로 버전도 맞춰 둔다.
requests==2.32.5
pandas==2.3.3
numpy==2.0.2
scikit-learn==1.6.1
joblib==1.5.3

# 사이트 생성. 로컬에 있다는 것은 근거가 아니다 — 실행에 필요한 것은 전부 적는다.
Jinja2==3.1.6
PyYAML==6.0.3

# 공유 venv(horseai)에는 없다. 테스트를 돌리려면 이것부터 깔아야 한다.
pytest==8.3.4
